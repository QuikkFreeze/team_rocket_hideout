const stripe = Stripe('<%= Rails.configuration.stripe[:publishable_key] %>');

stripe.redirectToCheckout({
  sessionId: '<%= @session.id %>',
  clientReferenceId: '<%= @ord.id %>'
}).then(function(result){
  alert(result.error.message);
});